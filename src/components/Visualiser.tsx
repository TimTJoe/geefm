import styled from "@emotion/styled"
import { Box } from "@mui/material"


const Container = styled(Box)`
    display: grid;
    place-items: center;
`

const Visualiser = () => {
    return (<Container>visual</Container>)
}

export default Visualiser